<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ma.ensaf.blooddonationapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:theme="@style/Theme.BloodDonationApp">
        <activity android:name=".fragments.thirdPartyLogin.SignUpRecipientFragTP"></activity>
        <activity android:name=".fragments.thirdPartyLogin.SignUpDonorFragTP" />
        <activity android:name=".fragments.thirdPartyLogin.thirdPartySignUp" />
        <activity android:name=".activities.AboutActivity" />
        <activity android:name=".activities.chat.MainChatActivity" />
        <activity android:name=".activities.chat.ChatActivity" />
        <activity android:name=".activities.NewMainActivity" />
        <activity android:name=".activities.NewLoginActivity" />
        <activity android:name=".activities.Settings.ChangePhoneNumberActivity" />
        <activity android:name=".activities.Settings.ChangePasswordActivity" />
        <activity android:name=".activities.Settings.ChangeEmailActivity" />
        <activity android:name=".activities.Settings.EditProfileActivity" />
        <activity android:name=".activities.Settings.SettingsActivity" />
        <activity android:name=".activities.Settings.ForgotPasswordActivity" />
        <activity android:name=".activities.CategorySelectedActivity" />
        <activity android:name=".activities.Settings.ProfileActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>